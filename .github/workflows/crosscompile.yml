name: Build Joplin for Windows ARM64

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-arm64:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      # 1. Standard install (allows keytar and sqlite3 native modules to build/fetch)
      - name: Install dependencies
        run: yarn install

      # 2. Build Monorepo Shared Libraries FIRST (@joplin/utils, @joplin/lib)
      - name: Build Monorepo Shared Libraries
        run: yarn run buildParallel

      # 3. Build Desktop App assets (esbuild can now find keytar.node)
      - name: Build Desktop App
        run: |
          cd packages/app-desktop
          yarn run build

      # 4. Package app for Windows ARM64
      - name: Package Desktop App for WOA
        run: |
          cd packages/app-desktop
          yarn run dist --win --arm64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm_config_arch: arm64

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: joplin-win-arm64
          path: packages/app-desktop/dist/*.exe

      - name: Publish to GitHub Releases
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          gh release create ${{ github.ref_name }} packages/app-desktop/dist/*.exe --title "Joplin WOA ${{ github.ref_name }}" --notes "Automated Windows on ARM (ARM64) build for Joplin."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
